describe('startmeup', () => {
  describe('Mapping args', () => {
    test.todo('Calls printusage if mappedArgs.empty is set to true')
    test.todo('Throws when args are invalid')
  })
  
  describe('starter retrieval', () => {
    test.todo('If mappedArgs.possibleBundleUrl is set, fetcher is called to try and retrieve it')
      test.todo('If fetcher cannot get mappedArgs.possibleBundleUrl, gitCloner retrieves repo')
      test.todo('')
  })
})